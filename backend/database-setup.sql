-- Create the database
CREATE DATABASE socialapp;

-- Connect to the database (you'll need to do this manually in pgAdmin or psql)
-- \c socialapp

-- The tables will be created automatically by Spring Boot when you run the application
-- due to spring.jpa.hibernate.ddl-auto=update in application.properties
